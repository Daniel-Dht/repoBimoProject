{% extends 'layout.html.twig' %}


{% block title %}
  Ajouter un Séjour d'Hospitalisation - {{ parent() }}
{% endblock %}

{% block body %}

	{# On n'affiche le lien « Ajouter une annonce » qu'aux auteurs
  (et admins, qui héritent du rôle auteur) #}
	{% if is_granted('ROLE_ADMIN') %}
		<div class="index">

	 		<h2>Ajouter un Séjour d'Hospitalisation</h2>

  			{{ include("BimoBundle:Hosp:formHosp.html.twig") }}

	 	</div>
	{% endif %}

	<p>
		<a href="{{ path('viewPatient', {'id' : patient.id }) }}" class="btn btn-primary">
	      <i class="glyphicon glyphicon-chevron-left"></i>
	      Retour à la liste
	    </a>
   </p>
   
{% endblock %}
